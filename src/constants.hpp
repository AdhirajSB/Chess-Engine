#pragma once

inline constexpr int screenWidth = 960;
inline constexpr int screenHeight = 960;
inline constexpr int squareSize = screenWidth / 8;
inline constexpr int barWidth = 20;
inline constexpr int padding = 1;
inline constexpr int offsetX = barWidth + (padding * 2);
inline constexpr int offsetY = 0;